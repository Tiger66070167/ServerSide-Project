<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Project Management: Register</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/auth.css">
</head>
<body>
  <div class="auth-container">
    <div class="auth-form-card">
      <h2>Register</h2>
      <form method="POST" action="/register">
        <div class="input-group">
          <label for="username">Username</label>
          <input type="text" id="username" name="username" required />
        </div>
        <div class="input-group">
          <label for="email">Email</label>
          <input type="email" id="email" name="email" required />
        </div>
        <div class="input-group">
          <label for="password">Password</label>
          <input type="password" id="password" name="password" required />
        </div>
        <div class="input-group">
          <label for="confirmPassword">Confirm Password</label>
          <input type="password" id="confirmPassword" name="confirmPassword" required />
        </div>
        <button type="submit" class="auth-button">Register</button>
      </form>

      <div id="form-messages">
        <% if (message) { %>
          <p class="success-message"><%= message %></p>
        <% } %>
        <% if (error) { %>
          <p class="error-message"><%= error %></p>
        <% } %>
      </div>

      <p class="auth-switch">Already have an account? <a href="/login">Login</a></p>
    </div>
  </div>

  <!-- Script to scroll to messages if they exist -->
  <script>
    // รอให้หน้าเว็บโหลดเสร็จก่อน
    document.addEventListener('DOMContentLoaded', () => {
      // ค้นหา element ที่เราสร้างไว้
      const messagesContainer = document.getElementById('form-messages');

      // ตรวจสอบว่าใน container นี้มีข้อความแสดงอยู่หรือไม่ (เช็คจาก element ลูก)
      if (messagesContainer && messagesContainer.children.length > 0) {
        // ถ้ามีข้อความ ให้เลื่อนหน้าจอไปที่ตำแหน่งของข้อความนั้นอย่างนุ่มนวล
        messagesContainer.scrollIntoView({ behavior: 'smooth', block: 'center' });
      }
    });
  </script>
</body>
</html>
